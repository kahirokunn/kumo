FROM alpine:3.20

RUN apk add --no-cache ca-certificates \
    && addgroup -S kumo && adduser -S kumo -G kumo \
    && mkdir -p /data && chown kumo:kumo /data

COPY kumo /usr/local/bin/kumo

USER kumo

EXPOSE 4566

ENTRYPOINT ["kumo"]
CMD ["--host", "0.0.0.0", "--port", "4566"]
